<!DOCTYPE html>
<html lang="en">
<>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Responsive Layout</title>
    <styl>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #2b0a0a; /* quase preto com toque vinho */
            color: #a63d40; /* vermelho escuro, vinho queimado */
            line-height: 1.6;
        }

        header {
            background-color: #a33d3d; /* bordô escuro */
            padding: 20px 0;
            box-shadow: 0 4px 6px rgba(219, 43, 48, 0.6);
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            overflow-x: hidden;
        }

        nav ul {
            display: flex;
            justify-content: center;
            list-style: none;
            gap: 20px;
            flex-wrap: wrap;
            padding: 0 10px;
        }

        nav ul li a {
            text-decoration: none;
            color: #000000;
            font-weight: bold;
            padding: 10px 15px;
            transition: background 0.3s ease, color 0.3s ease;
            border-radius: 8px;
            display: block;
            white-space: nowrap;
        }

        nav ul li a:hover {
            background-color: #7a292a; /* vermelho escuro mais suave */
            color: #fff;
        }

        .titulo {
            text-align: center;
            font-size: clamp(1.8rem, 5vw, 2.5rem);
            margin: 30px 20px 0;
            color: #531113;
            line-height: 1.3;
        }

        .imagem-centralizada {
            display: flex;
            justify-content: center;
            align-items: center;
            margin: clamp(20px, 5vw, 40px) 20px;
            padding: 0 10px;
        }

        .imagem-centralizada img {
            max-width: 100%;
            height: auto;
            border-radius: 12px; 
            box-shadow: 0 4px 8px rgba(166, 61, 64, 0.6);
        }

        /* Responsive adjustments */
        @media (max-width: 992px) {
            .container {
                padding: 0 15px;
            }
        }

        @media (max-width: 768px) {
            nav ul {
                gap: 10px;
            }
            
            nav ul li a {
                padding: 8px 12px;
                font-size: 0.9em;
            }

            .titulo {
                font-size: 2rem;
            }
        }

        @media (max-width: 480px) {
            nav ul {
                flex-direction: column;
                align-items: center;
                gap: 5px;
            }
            
            nav ul li a {
                width: 100%;
                text-align: center;
                padding: 10px;
            }

            .titulo {
                font-size: 1.5rem;
            }

            .imagem-centralizada {
                margin: 20px 0;
            }
        }